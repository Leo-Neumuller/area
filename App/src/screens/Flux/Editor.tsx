import React, { useState } from 'react';
import { Text, View } from 'react-native';

export const Editor: React.FC = () => {
    return (
        <View>
            <Text>editor</Text>
        </View>
    )
}