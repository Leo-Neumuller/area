import Close__SvelteComponent_ from "./+Close.svelte";

export interface ChildProps {
    className: string;
    color: string;
}
export default Close__SvelteComponent_;

export const prerender = true;